import { ArrowRight, LoaderCircle, MapPin, Search } from 'lucide-react';
import { popularLocations } from '../../data/geeMockData';
export default function GEESearch({ value, onChange, onSearch, loading, selected }) { return <section className="gee-search-wrap gee-container" id="location-search"><div className="gee-search-card"><div className="gee-section-kicker">START WITH A LOCATION</div><h2>Search Location</h2><p>Analyze a district, village or pincode using satellite data.</p><form className="gee-search-form" onSubmit={(event) => { event.preventDefault(); onSearch(value); }}><div className="gee-search-input"><Search size={19} /><input value={value} onChange={(event) => onChange(event.target.value)} placeholder="Search district, village or pincode..." aria-label="Search district, village or pincode" /><button type="submit" disabled={loading || !value.trim()}>{loading ? <LoaderCircle className="gee-spin" size={18} /> : <>Search <ArrowRight size={17} /></>}</button></div></form><div className="gee-popular"><span>Popular searches</span>{popularLocations.map((location) => <button type="button" key={location} onClick={() => { onChange(location); onSearch(location); }}><MapPin size={13} />{location}</button>)}</div>{loading && <div className="gee-search-status" role="status"><LoaderCircle className="gee-spin" size={16} /> Searching satellite data...</div>}{selected && !loading && <div className="gee-selected"><MapPin size={17} /><span><strong>Selected Location:</strong> {selected.name}, {selected.state}</span><span className="gee-source-badge">● GEE ready</span></div>}</div></section>; }
