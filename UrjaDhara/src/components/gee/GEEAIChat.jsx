import { Bot, MessageCircle, Send, X } from 'lucide-react';
import { useState } from 'react';
const prompts = ['Which district needs solar urgently?', "Explain Raipur's GEE analysis", 'Which areas have high water stress?', 'Recommend a solar-water solution'];
export default function GEEAIChat() { const [open, setOpen] = useState(false); const [message, setMessage] = useState(''); return <>{open && <aside className="gee-chat-drawer" aria-label="UrjaDhara AI assistant"><div className="gee-chat-header"><div><Bot size={20} /><strong>UrjaDhara AI</strong></div><button onClick={() => setOpen(false)} aria-label="Close AI assistant"><X size={19} /></button></div><div className="gee-chat-body"><div className="gee-chat-message"><Bot size={18} /><p>Hi! I can help turn satellite intelligence into a planning decision.</p></div><span className="gee-chat-label">Suggested prompts</span>{prompts.map((prompt) => <button key={prompt} onClick={() => setMessage(prompt)}>{prompt}</button>)}</div><form className="gee-chat-input" onSubmit={(event) => event.preventDefault()}><input value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Ask about this analysis..." aria-label="Ask UrjaDhara AI" /><button aria-label="Send question"><Send size={17} /></button></form></aside>}<button className="gee-ai-fab" onClick={() => setOpen(!open)}><MessageCircle size={19} /> Ask UrjaDhara AI</button></>; }
