export default function DataSources({ sources = [] }) { return <section className="gee-section gee-container"><div className="gee-heading"><div><div className="gee-section-kicker">TRANSPARENT EVIDENCE</div><h2>Satellite Data Sources</h2></div><span className="gee-data-badge">● Powered by Google Earth Engine</span></div>{sources.length ? <div className="gee-sources-table-wrap"><table className="gee-sources-table"><thead><tr><th>Dataset</th><th>Provider</th><th>Indicator</th><th>Resolution</th><th>Period</th></tr></thead><tbody>{sources.map((source) => <tr key={`${source.name}-${source.indicator}`}><td>{source.name}</td><td>{source.provider}</td><td>{source.indicator}</td><td>{source.resolution || 'Not available'}</td><td>{source.period}</td></tr>)}</tbody></table></div> : <div className="gee-empty-state">No satellite data source metadata was returned for this analysis.</div>}</section>; }
