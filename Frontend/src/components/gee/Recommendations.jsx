import { ArrowRight, Droplets, Leaf, LoaderCircle, School, Sun } from 'lucide-react';

const icons = { solar: Sun, water: Droplets, environment: Leaf, facilities: School };
export default function Recommendations({ items = [], loading, error }) {
  return <section className="gee-section gee-container"><div className="gee-heading"><div><div className="gee-section-kicker">FROM INSIGHT TO IMPACT</div><h2>Recommended Actions</h2></div><p>Recommendations are generated from the selected location's backend analysis.</p></div>{loading && <div className="gee-analysis-loading" role="status"><LoaderCircle className="gee-spin" size={20} /> Loading recommendations...</div>}{error && <div className="gee-error" role="alert">{error}</div>}{!loading && !error && <div className="gee-recommend-grid">{items.map((item) => { const Icon = icons[item.type] || School; return <article className={`gee-recommend-card ${item.type || 'green'}`} key={`${item.type}-${item.title}`}><div className="gee-recommend-icon"><Icon size={24} /></div><h3>{item.title}</h3><p>{item.reason}</p><small className="gee-recommend-priority">Priority: {item.priority || 'Not available'}</small><button className="gee-text-button">Review action <ArrowRight size={16} /></button></article>; })}</div>}</section>;
}
