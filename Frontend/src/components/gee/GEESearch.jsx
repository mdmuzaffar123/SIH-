import { ArrowRight, LoaderCircle, MapPin, Search } from 'lucide-react';
export default function GEESearch({ value, onChange, onSearch, loading, selected, results = [], searching }) { return <section className="gee-search-wrap gee-container" id="location-search"><div className="gee-search-card"><div className="gee-section-kicker">START WITH A LOCATION</div><h2>Search Location</h2><p>Analyze a district, village or location using satellite data.</p><form className="gee-search-form" onSubmit={(event) => { event.preventDefault(); onSearch(value); }}><div className="gee-search-input"><Search size={19} /><input value={value} onChange={(event) => onChange(event.target.value)} placeholder="Search district, village or location..." aria-label="Search district, village or location" /><button type="submit" disabled={loading || !value.trim()}>{loading ? <LoaderCircle className="gee-spin" size={18} /> : <>Search <ArrowRight size={17} /></>}</button></div></form>{searching && <div className="gee-search-status" role="status"><LoaderCircle className="gee-spin" size={16} /> Finding locations...</div>}{results.length > 0 && !loading && <div className="gee-search-results" role="listbox" aria-label="Location suggestions">{results.map((result) => <button type="button" role="option" key={`${result.type}-${result.name}-${result.state}`} onClick={() => onSearch(result)}><MapPin size={15} /><span><strong>{result.name}</strong><small>{result.district} District · {result.state}</small></span><ArrowRight size={15} /></button>)}</div>}{selected && !loading && <div className="gee-selected"><MapPin size={17} /><span><strong>Selected Location:</strong> {selected.name}, {selected.state}</span><span className="gee-source-badge">● GEE ready</span></div>}</div></section>; }
