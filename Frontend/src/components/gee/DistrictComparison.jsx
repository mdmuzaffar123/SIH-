import { useState } from 'react';
import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { ArrowRight, GitCompareArrows } from 'lucide-react';
import { comparisonChartData, comparisonRows, geeLocations } from '../../data/geeMockData';
export default function DistrictComparison() { const [districts, setDistricts] = useState(['Raipur', 'Bastar', 'Dantewada']); const toggle = (name) => setDistricts((current) => current.includes(name) ? (current.length > 1 ? current.filter((item) => item !== name) : current) : current.length < 3 ? [...current, name] : current); const names = [...districts, ...['Raipur', 'Bastar', 'Dantewada'].filter((name) => !districts.includes(name))].slice(0, 3); return <section className="gee-section gee-compare-section"><div className="gee-container"><div className="gee-heading"><div><div className="gee-section-kicker">MAKE THE CASE FOR INVESTMENT</div><h2>Compare Districts</h2></div><p>Where should government investment happen first?</p></div><div className="gee-compare-controls">{Object.keys(geeLocations).map((name) => <button type="button" className={districts.includes(name) ? 'active' : ''} onClick={() => toggle(name)} key={name}><GitCompareArrows size={15} /> {name}</button>)}</div><div className="gee-compare-layout"><div className="gee-table-wrap"><table><thead><tr><th>Indicator</th>{names.map((name) => <th key={name}>{name}</th>)}</tr></thead><tbody>{comparisonRows.map((row) => <tr key={row.label}><td>{row.label}</td>{names.map((name) => <td key={name}>{row.values[['Raipur', 'Bastar', 'Dantewada'].indexOf(name)] || geeLocations[name].priority}</td>)}</tr>)}</tbody></table></div><div className="gee-chart-card"><h3>Relative need score</h3><ResponsiveContainer width="100%" height={230}><BarChart data={comparisonChartData}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e2eee5" /><XAxis dataKey="indicator" tick={{ fontSize: 11 }} /><YAxis domain={[0, 100]} tick={{ fontSize: 11 }} /><Tooltip /><Legend iconType="circle" /><Bar dataKey="Raipur" fill="#86efac" radius={[5, 5, 0, 0]} /><Bar dataKey="Bastar" fill="#22c55e" radius={[5, 5, 0, 0]} /><Bar dataKey="Dantewada" fill="#f59e0b" radius={[5, 5, 0, 0]} /></BarChart></ResponsiveContainer></div></div><button className="gee-text-button gee-compare-cta">Open district planning view <ArrowRight size={16} /></button></div></section>; }
